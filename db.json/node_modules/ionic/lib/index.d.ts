import { IProject, IonicContext, IonicEnvironment } from 'ionic/definitions';
export declare function generateIonicEnvironment(ctx: IonicContext, pargv: string[]): Promise<{
    env: IonicEnvironment;
    project?: IProject;
}>;
