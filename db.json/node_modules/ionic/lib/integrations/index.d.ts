import { BaseConfig } from '@ionic/cli-framework';
import { IConfig, IIntegration, ILogger, IProject, IShell, InfoItem, IntegrationAddDetails, IntegrationName, ProjectIntegration, ProjectPersonalizationDetails } from 'ionic/definitions';
import * as ζcapacitor from 'ionic/lib/integrations/capacitor';
import * as ζcordova from 'ionic/lib/integrations/cordova';
import * as ζenterprise from 'ionic/lib/integrations/enterprise';
export { INTEGRATION_NAMES } from 'ionic/guards';
export interface IntegrationOptions {
    quiet?: boolean;
}
export interface IntegrationDeps {
    readonly config: IConfig;
    readonly shell: IShell;
    readonly project: IProject;
    readonly log: ILogger;
}
export declare type IntegationUnion = ζcapacitor.Integration | ζcordova.Integration | ζenterprise.Integration;
export declare class IntegrationConfig extends BaseConfig<ProjectIntegration> {
    provideDefaults(c: Partial<Readonly<ProjectIntegration>>): ProjectIntegration;
}
export declare abstract class BaseIntegration<T extends ProjectIntegration> implements IIntegration<T> {
    protected readonly e: IntegrationDeps;
    abstract readonly name: IntegrationName;
    abstract readonly summary: string;
    abstract readonly archiveUrl?: string;
    abstract readonly config: BaseConfig<T>;
    constructor(e: IntegrationDeps);
    static createFromName(deps: IntegrationDeps, name: 'capacitor'): Promise<ζcapacitor.Integration>;
    static createFromName(deps: IntegrationDeps, name: 'cordova'): Promise<ζcordova.Integration>;
    static createFromName(deps: IntegrationDeps, name: 'enterprise'): Promise<ζenterprise.Integration>;
    static createFromName(deps: IntegrationDeps, name: IntegrationName): Promise<IIntegration<ProjectIntegration>>;
    getInfo(): Promise<InfoItem[]>;
    isAdded(): boolean;
    isEnabled(): boolean;
    enable(config?: ProjectIntegration): Promise<void>;
    disable(): Promise<void>;
    personalize(details: ProjectPersonalizationDetails): Promise<void>;
    add(details: IntegrationAddDetails): Promise<void>;
}
