import { CommandMetadata } from 'ionic/definitions';
import { Command } from 'ionic/lib/command';
export declare class StateCommand extends Command {
    getMetadata(): Promise<CommandMetadata>;
    run(): Promise<void>;
}
