import { CommandMetadata } from 'ionic/definitions';
import { RunCommand } from 'ionic/commands/cordova/run';
export declare class EmulateCommand extends RunCommand {
    getMetadata(): Promise<CommandMetadata>;
}
